name: intercepted_client
description: Interceptors for http package that support concurrent requests.
version: 0.0.1-dev.7
repository: https://github.com/hawkkiller/intercepted_client

environment:
  sdk: ^3.0.5

# Add regular dependencies here.
dependencies:
  collection: ^1.18.0
  http: ^1.1.0
  stack_trace: ^1.11.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
